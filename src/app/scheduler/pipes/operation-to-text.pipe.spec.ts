import { OperationToTextPipe } from './operation-to-text.pipe';

describe('OperationToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new OperationToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
